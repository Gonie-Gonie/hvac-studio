# Studio GUI

Reserved for the Wails/React authoring layer.

The GUI must edit and visualize `project.bcsproj`, `graph.json`, component source files, schemas, and runtime outputs. It must call `bcs-runner` for validation and execution rather than becoming a separate simulation engine.

