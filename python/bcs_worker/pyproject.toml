[project]
name = "bcs-worker"
version = "0.1.0"
description = "Python worker for HVAC Studio user-defined components"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

