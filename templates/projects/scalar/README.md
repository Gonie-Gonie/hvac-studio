# Scalar Project Template

This template creates a minimal runnable project with one Python component, one public input, one public output, and a default input case.

It is intentionally small so new projects start from valid source-of-truth files rather than GUI-only state.
